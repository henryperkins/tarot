import { drawSampleReading } from './cards';

export const SAMPLE_READING = drawSampleReading(3);

export const SAMPLE_NARRATIVE = `The cards suggest a shift from curiosity into focus.
Lean into what feels exciting, but slow your pace enough to honor the intuition that is already guiding you.`;
